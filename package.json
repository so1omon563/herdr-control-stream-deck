{
  "name": "herdr-control-stream-deck",
  "version": "0.1.0",
  "private": true,
  "description": "Herdr workspace, tab, pane, and agent controls for Elgato Stream Deck hardware.",
  "scripts": {
    "build:profiles": "zsh scripts/build-profiles.zsh",
    "test": "node validate.mjs"
  }
}
